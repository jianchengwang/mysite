version: '3'
services:
  coderunner:
    image: jianchengwang/mysite:latest
    ports:
      - "8081:8081"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /root/mysite:/root/mysite
    environment:
